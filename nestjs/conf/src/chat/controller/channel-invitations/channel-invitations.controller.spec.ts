import { Test, TestingModule } from '@nestjs/testing';
import { ChannelInvitationsController } from './channel-invitations.controller';

describe('ChannelInvitationsController', () => {
  let controller: ChannelInvitationsController;

//   beforeEach(async () => {
//     const module: TestingModule = await Test.createTestingModule({
//       controllers: [ChannelInvitationsController],
//     }).compile();

//     controller = module.get<ChannelInvitationsController>(
//       ChannelInvitationsController,
//     );
//   });

//   it('should be defined', () => {
//     expect(controller).toBeDefined();
//   });
it.skip('Not writing tests is a bad practice', () => {});
});
