import { Test, TestingModule } from '@nestjs/testing';
import { ChannelInvitationsService } from './channel-invitations.service';

describe('ChannelInvitationsService', () => {
  let service: ChannelInvitationsService;

  //   beforeEach(async () => {
  //     const module: TestingModule = await Test.createTestingModule({
  //       providers: [ChannelInvitationsService],
  //     }).compile();

  //     service = module.get<ChannelInvitationsService>(ChannelInvitationsService);
  //   });

  //   it('should be defined', () => {
  //     expect(service).toBeDefined();
  //   });

  it.skip('Not writing tests is a bad practice', () => {});
});
